# Configuration file for database connection settings

# Dictionary containing database connection details
DB_CONFIG = {
    "host": "127.0.0.1",  # Database server address (localhost for local development)
    "port": 3307,  # MySQL/MariaDB port (default is 3306, changed if needed)
    "user": "root",  # MySQL root user
    "password": "",  # MySQL password (empty if no password is set)
    "database": "bills_2025"  # Name of the database used in the project
}
